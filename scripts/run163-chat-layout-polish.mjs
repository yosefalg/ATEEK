import fs from 'node:fs';

const file = 'src/components/SpatialDealScreens.tsx';
let s = fs.readFileSync(file, 'utf8');

const replace = (from, to, label) => {
  if (!s.includes(from)) throw new Error(`Run163 chat layout anchor missing: ${label}`);
  s = s.replace(from, to);
};

replace(
  "  const [mediaBusy, setMediaBusy] = useState(false);",
  "  const [mediaBusy, setMediaBusy] = useState(false);\n  const [offerSheetOpen, setOfferSheetOpen] = useState(false);",
  'offer sheet state',
);

replace(
  "  const counterpart = m.profiles.find((x) => x.id === counterpartId);",
  "  const counterpart = m.profiles.find((x) => x.id === counterpartId);\n  const threadOffers = useMemo(() => m.offers.filter((x) => x.thread_id === thread), [m.offers, thread]);",
  'thread offers memo',
);

const offerStart = '      <View style={styles.offerStrip}>';
const composerStart = '      <View style={styles.composerDock}>';
const offerStartIndex = s.indexOf(offerStart);
const composerStartIndex = s.indexOf(composerStart, offerStartIndex);
if (offerStartIndex < 0 || composerStartIndex < 0) throw new Error('Run163 chat layout anchor missing: persistent offer stack');

const compactNegotiation = `      <View style={styles.chatSafetyNote} accessible accessibilityRole="text">\n        <Ionicons name="shield-checkmark-outline" size={17} color={CYAN} />\n        <Text style={styles.chatSafetyText}>تفاوض بأمان ولا تشارك رموز التحقق أو بيانات الدفع داخل المحادثة.</Text>\n      </View>\n\n      <View style={styles.dealToolsRow}>\n        <Pressable\n          accessibilityRole="button"\n          accessibilityLabel={\`العروض والتفاوض، عدد العروض \${threadOffers.length}\`}\n          accessibilityHint="يفتح لوحة تقديم العرض ومراجعة العروض الحالية"\n          onPress={() => { haptics.light(); setOfferSheetOpen(true); }}\n          style={({ pressed }) => [styles.dealToolsButton, pressed && styles.pressed]}\n        >\n          <Ionicons name="pricetag-outline" size={17} color={CYAN} />\n          <Text style={styles.dealToolsText}>العروض والتفاوض</Text>\n          {threadOffers.length > 0 ? <View style={styles.dealCount}><Text style={styles.dealCountText}>{threadOffers.length}</Text></View> : null}\n        </Pressable>\n      </View>\n\n`;

s = s.slice(0, offerStartIndex) + compactNegotiation + s.slice(composerStartIndex);

const dmCloseAnchor = `      </View>\n    </KeyboardAvoidingView>\n  );\n}\n\nfunction MetricCard`;
const dmCloseReplacement = `      </View>\n\n      <Modal visible={offerSheetOpen} transparent animationType="slide" onRequestClose={() => setOfferSheetOpen(false)}>\n        <KeyboardAvoidingView style={styles.offerSheetBackdrop} behavior={Platform.OS === 'ios' ? 'padding' : 'height'}>\n          <Pressable accessibilityLabel="إغلاق لوحة العروض" style={StyleSheet.absoluteFill} onPress={() => setOfferSheetOpen(false)} />\n          <View style={styles.offerSheet}>\n            <View style={styles.sheetGrabber} />\n            <View style={styles.offerSheetHeader}>\n              <View style={styles.offerSheetHeading}>\n                <Text style={styles.offerSheetTitle}>العروض والتفاوض</Text>\n                <Text style={styles.offerSheetSub}>قدّم عرضك وراجع حالة العروض بدون تشويش شاشة الرسائل.</Text>\n              </View>\n              <Pressable accessibilityRole="button" accessibilityLabel="إغلاق العروض" onPress={() => setOfferSheetOpen(false)} style={styles.offerSheetClose}>\n                <Ionicons name="close" size={20} color={INK} />\n              </Pressable>\n            </View>\n\n            <View style={styles.offerInputRow}>\n              <TextInput\n                accessibilityLabel="عرضك بالدينار العراقي"\n                value={price}\n                onChangeText={setPrice}\n                keyboardType="number-pad"\n                placeholder="عرضك بالدينار"\n                placeholderTextColor="#697386"\n                style={styles.offerInput}\n              />\n              <Pressable\n                accessibilityRole="button"\n                accessibilityLabel="تقديم العرض"\n                disabled={busy}\n                onPress={sendOffer}\n                style={({ pressed }) => [styles.offerButton, pressed && styles.pressed, busy && styles.disabled]}\n              >\n                <Ionicons name="pricetag-outline" size={16} color={OBSIDIAN} />\n                <Text style={styles.offerButtonText}>تقديم عرض</Text>\n              </Pressable>\n            </View>\n\n            <ScrollView style={styles.offerSheetList} contentContainerStyle={styles.offerSheetListContent} keyboardShouldPersistTaps="handled">\n              {threadOffers.length ? threadOffers.map((offer) => (\n                <View key={offer.id} style={styles.offerCard}>\n                  <View style={{ flex: 1 }}>\n                    <Text style={styles.offerValue}>{formatPrice(Number(offer.amount))}</Text>\n                    <Text style={styles.offerStatus}>{({ pending: 'بانتظار الرد', accepted: 'متفق عليه', rejected: 'مرفوض', superseded: 'استُبدل بعرض جديد', completed: 'صفقة مكتملة' } as Record<string, string>)[offer.status] ?? offer.status}</Text>\n                  </View>\n                  {offer.status === 'pending' && offer.author_id !== m.user.id ? (\n                    <View style={styles.offerActions}>\n                      <Pressable\n                        disabled={busy}\n                        onPress={() => Alert.alert('تأكيد الاتفاق؟', 'سيصبح الإعلان غير متاح للعروض الجديدة.', [\n                          { text: 'إلغاء' },\n                          { text: 'قبول', onPress: () => { haptics.success(); void run('respond', { offer_id: offer.id, status: 'accepted' }); } },\n                        ])}\n                        style={styles.miniAccept}\n                      >\n                        <Text style={styles.miniAcceptText}>قبول</Text>\n                      </Pressable>\n                      <Pressable disabled={busy} onPress={() => void run('respond', { offer_id: offer.id, status: 'rejected' })} style={styles.miniReject}>\n                        <Text style={styles.miniRejectText}>رفض</Text>\n                      </Pressable>\n                    </View>\n                  ) : null}\n                  {offer.status === 'accepted' && t?.buyer_id === m.user.id ? (\n                    <Pressable\n                      disabled={busy}\n                      onPress={() => Alert.alert('استلمت السلعة؟', 'أكّد فقط بعد استلامها وفحصها.', [\n                        { text: 'إلغاء' },\n                        { text: 'نعم، استلمتها', onPress: () => { haptics.success(); void run('complete', { offer_id: offer.id }); } },\n                      ])}\n                      style={styles.completeButton}\n                    >\n                      <Text style={styles.completeButtonText}>تأكيد الاستلام</Text>\n                    </Pressable>\n                  ) : null}\n                </View>\n              )) : (\n                <View style={styles.offerEmpty}>\n                  <Ionicons name="pricetag-outline" size={25} color="#687284" />\n                  <Text style={styles.offerEmptyTitle}>لا توجد عروض بعد</Text>\n                  <Text style={styles.offerEmptyText}>يمكنك بدء التفاوض من الحقل أعلاه.</Text>\n                </View>\n              )}\n            </ScrollView>\n          </View>\n        </KeyboardAvoidingView>\n      </Modal>\n    </KeyboardAvoidingView>\n  );\n}\n\nfunction MetricCard`;
replace(dmCloseAnchor, dmCloseReplacement, 'offer sheet modal');

const styleStart = "  offerStrip: { marginHorizontal: 10, marginBottom: 6, flexDirection: 'row-reverse', gap: 8 },";
const composerStyleStart = "  composerDock: { margin: 10, marginTop: 6, padding: 8, borderRadius: 22, borderWidth: 1, borderColor: '#343C49', backgroundColor: '#11151D', flexDirection: 'row', alignItems: 'flex-end', gap: 6, paddingBottom: Platform.OS === 'android' ? 10 : 8 },";
const styleStartIndex = s.indexOf(styleStart);
const composerStyleIndex = s.indexOf(composerStyleStart, styleStartIndex);
if (styleStartIndex < 0 || composerStyleIndex < 0) throw new Error('Run163 chat layout anchor missing: offer styles');

const offerStyles = `  chatSafetyNote: { marginHorizontal: 10, marginTop: 3, marginBottom: 5, minHeight: 42, borderRadius: 15, borderWidth: 1, borderColor: '#235147', backgroundColor: '#0F1C1A', paddingHorizontal: 11, paddingVertical: 8, flexDirection: 'row-reverse', alignItems: 'center', gap: 8 },\n  chatSafetyText: { flex: 1, color: '#A8B7B2', fontSize: 9, lineHeight: 15, textAlign: 'right' },\n  dealToolsRow: { paddingHorizontal: 10, paddingBottom: 4 },\n  dealToolsButton: { minHeight: 42, alignSelf: 'flex-end', borderRadius: 14, borderWidth: 1, borderColor: '#2D4E48', backgroundColor: '#101A19', paddingHorizontal: 12, flexDirection: 'row-reverse', alignItems: 'center', gap: 7 },\n  dealToolsText: { color: '#D7EEE7', fontSize: 10, fontWeight: '900' },\n  dealCount: { minWidth: 22, height: 22, borderRadius: 11, paddingHorizontal: 6, backgroundColor: CYAN, alignItems: 'center', justifyContent: 'center' },\n  dealCountText: { color: OBSIDIAN, fontSize: 9, fontWeight: '900' },\n  offerSheetBackdrop: { flex: 1, justifyContent: 'flex-end', backgroundColor: 'rgba(0,0,0,0.58)' },\n  offerSheet: { maxHeight: '76%', borderTopLeftRadius: 28, borderTopRightRadius: 28, borderWidth: 1, borderColor: '#343D49', backgroundColor: '#0F1219', padding: 14, paddingBottom: Platform.OS === 'ios' ? 30 : 18 },\n  offerSheetHeader: { minHeight: 52, flexDirection: 'row-reverse', alignItems: 'center', gap: 10, marginBottom: 10 },\n  offerSheetHeading: { flex: 1, alignItems: 'flex-end' },\n  offerSheetTitle: { color: INK, fontSize: 18, fontWeight: '900', textAlign: 'right' },\n  offerSheetSub: { color: MUTED, fontSize: 9, lineHeight: 15, textAlign: 'right', marginTop: 3 },\n  offerSheetClose: { width: 40, height: 40, borderRadius: 13, borderWidth: 1, borderColor: LINE, backgroundColor: TITANIUM, alignItems: 'center', justifyContent: 'center' },\n  offerInputRow: { flexDirection: 'row-reverse', gap: 8, marginBottom: 10 },\n  offerInput: { flex: 1, height: 48, borderRadius: 15, borderWidth: 1, borderColor: LINE, backgroundColor: '#11151D', color: INK, paddingHorizontal: 12, textAlign: 'right' },\n  offerButton: { height: 48, borderRadius: 15, paddingHorizontal: 14, backgroundColor: CYAN, flexDirection: 'row-reverse', alignItems: 'center', justifyContent: 'center', gap: 6 },\n  offerButtonText: { color: OBSIDIAN, fontWeight: '900', fontSize: 11 },\n  offerSheetList: { maxHeight: 340 },\n  offerSheetListContent: { gap: 7, paddingBottom: 6 },\n  offerEmpty: { minHeight: 120, borderRadius: 17, borderWidth: 1, borderColor: LINE, backgroundColor: TITANIUM_SOFT, alignItems: 'center', justifyContent: 'center', padding: 16 },\n  offerEmptyTitle: { color: INK, fontSize: 13, fontWeight: '900', marginTop: 6 },\n  offerEmptyText: { color: MUTED, fontSize: 9, marginTop: 3 },\n  offerCard: { borderRadius: 15, borderWidth: 1, borderColor: LINE, backgroundColor: '#11151D', padding: 10, flexDirection: 'row-reverse', alignItems: 'center', gap: 8 },\n  offerValue: { color: GOLD, fontWeight: '900', fontSize: 13, textAlign: 'right' },\n  offerStatus: { color: MUTED, fontSize: 9, textAlign: 'right', marginTop: 2 },\n  offerActions: { flexDirection: 'row-reverse', gap: 5 },\n  miniAccept: { borderRadius: 10, backgroundColor: CYAN, paddingHorizontal: 10, paddingVertical: 7 },\n  miniAcceptText: { color: OBSIDIAN, fontSize: 9, fontWeight: '900' },\n  miniReject: { borderRadius: 10, borderWidth: 1, borderColor: '#66303A', paddingHorizontal: 10, paddingVertical: 7 },\n  miniRejectText: { color: DANGER, fontSize: 9, fontWeight: '900' },\n  completeButton: { borderRadius: 10, backgroundColor: GOLD, paddingHorizontal: 10, paddingVertical: 8 },\n  completeButtonText: { color: OBSIDIAN, fontSize: 9, fontWeight: '900' },\n`;

s = s.slice(0, styleStartIndex) + offerStyles + s.slice(composerStyleIndex);

fs.writeFileSync(file, s);
console.log('Run #163 chat layout polish applied: offers moved to negotiation sheet and chat timeline decluttered.');
